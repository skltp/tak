version: '3'
services:
  tak-web:
    build: ./tak-web
    depends_on:
      tak-mysql:
        condition: service_healthy
    ports:
      - "8001:8080"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./resources/etc/log4j2-console.xml:/etc/tak/log4j2-console.xml
    environment:
      # Uncomment to use Keycloak with DPoP, requires a Keycloak instance running on the same host
      spring.profiles.active: oauth-dpop
      spring.datasource.driver-class-name: com.mysql.cj.jdbc.Driver
      spring.datasource.url: jdbc:mysql://tak-mysql:3306/tp_admin?autoReconnect=true
      spring.datasource.username: tpadminuser
      spring.datasource.password: tpadminpassword
      spring.sql.init.platform: mysql
      logging.config: /etc/tak/log4j2-console.xml
      spring.session.jdbc.schema: "classpath:org/springframework/session/jdbc/schema-mysql.sql"
      spring.session.store-type: jdbc
      server.servlet.session.timeout: 30m
      spring.session.jdbc.initialize-schema: always
  tak-services:
    build: ./tak-services
    depends_on:
      tak-mysql:
        condition: service_healthy
    ports:
      - "8002:8080"
    volumes:
      - ./resources/etc/log4j2-console.xml:/etc/tak/log4j2-console.xml
    environment:
      spring.datasource.driver-class-name: com.mysql.cj.jdbc.Driver
      spring.datasource.url: jdbc:mysql://tak-mysql:3306/tp_admin?autoReconnect=true
      # DEV-TEST spring.datasource.url: jdbc:mysql://ind-ttjp-rh9-mysql1.ind1.sth.basefarm.net:3306/takv3?autoReconnect=true
      spring.datasource.username: tpadminuser
      spring.datasource.password: tpadminpassword
      # DEV-TEST spring.datasource.password: gp#bKmgNkFr7MEfgEFB7

  tak-monitor:
    build: ./tak-monitor
    depends_on:
      - tak-services
    volumes:
      - ./resources/etc/log4j2-console.xml:/etc/tak/log4j2-console.xml
    environment:
      spring.datasource.driver-class-name: com.mysql.cj.jdbc.Driver
      spring.datasource.url: jdbc:mysql://tak-mysql:3306/tp_admin?autoReconnect=true
      spring.datasource.username: tpadminuser
      spring.datasource.password: tpadminpassword
      logging.config: /etc/tak/log4j2-console.xml
      tak.monitor.reset.nodes.0.url: http://tak-services:8080/tak-services/reset/pv

  tak-mysql:
    image: mysql:8
    environment:
      MYSQL_RANDOM_ROOT_PASSWORD: yes
      MYSQL_DATABASE: tp_admin
      MYSQL_USER: tpadminuser
      MYSQL_PASSWORD: tpadminpassword
    ports:
      - "3306:3306"
    volumes:
      - ./resources/sql/tp-admin-testdb.sql:/docker-entrypoint-initdb.d/tp-admin-DDL.sql
    healthcheck:
      test: [ "CMD", "mysqladmin" ,"ping", "-h", "localhost" ]
      timeout: 20s
      retries: 10

  phpmyadmin:
    image: phpmyadmin
    restart: always
    ports:
      - 8081:80
    environment:
      PMA_HOST: ind-dtjp-rh8-mysql.ind1.sth.basefarm.net
      PMA_USER: tpadminuser
      PMA_PASSWORD: fpWf@VrgmoFnm2ajn&kC
